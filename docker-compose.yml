version: '3.9'

services:

  backend:
    build:
      context: ./Agendamente-Servicos-carro
    container_name: agendamento_backend
    ports:
      - "8080:8080"
    env_file:
      - ./Agendamente-Servicos-carro/.env
    extra_hosts:
      - "host.docker.internal:host-gateway"

